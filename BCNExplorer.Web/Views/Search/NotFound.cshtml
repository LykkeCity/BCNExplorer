@model LastBlockViewModel
@{
    ViewBag.Title = "Block Explorer";
    ViewBag.HideTopSearch = true;

}
<section class="section section--search section--padding">
    <div class="container container--extend">
        <div class="t-simulate">
            <div class="t-row">
                <div class="t-cell">
                    <h2>We didn't find anything...</h2>

                    <form method="get" action="@Url.Action("Search", "Search")" class="form form--default form--search_blockchain">
                        <input class="form-control" type="text" name="id" placeholder="Search transaction, assets, addresses or blocks...">
                        <button type="submit" class="btn btn-md"><i class="icon icon--search"></i> Search</button>
                    </form>
                    <p>
                        <small>Last block: <a href="@Url.Action("Index", "Block", new {id = Model.BlockId})">@Model.Height</a></small> - <small><a href="@Url.Action("AssetDirectiory", "Asset")">Asset Directory</a></small>
                    </p>
                </div>
            </div>
        </div>
    </div>
</section>